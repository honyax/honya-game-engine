# Day 13: 生OpenGLで三角形+シェーダー — バインディング体験完了

Phase 2 / ロードマップ該当行: 「生OpenGLで三角形+シェーダー — **バインディング体験完了**」

## 今日のゴール

**自作バインディングだけで、シェーダーで色づけした三角形が回る。**

Phase 2 のマイルストーン「自作バインディングだけで、シェーダーで色づけした三角形が出る」を今日で達成する。
Day 11 でウィンドウを、Day 12 でコンテキストを自分で作った。残っているのは
「頂点を GPU に置く」「シェーダを書く」「描けと命じる」の3つだけ。

そして今日は、**Phase 1 で自分の手で書いた処理が GPU では何になっているのか**が
1対1で見える日でもある。頂点シェーダは Day 6 の頂点変換、フラグメントシェーダは Day 8〜9 の
ピクセル処理、そして Day 3〜4 のラスタライズと補間は**触れない固定機能**として GPU の中にある。

Day 14 からは Silk.NET に移り、Phase 2 で書いた Win32/WGL/GL のコードは全部捨てる。
**捨てて構わないものを、それでも一度書いた**——それが Phase 2 の意味。

## 事前に読む資料

- [ゲームグラフィックス特論 A-2 GPU](https://tokoik.github.io/gg/) — ロードマップ指定。
  **今日の理論そのもの**。GPU のパイプラインと、どこがプログラマブルでどこが固定機能か
- [Hello Triangle — LearnOpenGL](https://learnopengl.com/Getting-started/Hello-Triangle) —
  今日書くコードの C++ 版。VAO / VBO の図解が分かりやすい
- [Rendering Pipeline Overview — OpenGL Wiki](https://www.khronos.org/opengl/wiki/Rendering_Pipeline_Overview) —
  要点1の対応表の一次情報
- [Vertex Specification — OpenGL Wiki](https://www.khronos.org/opengl/wiki/Vertex_Specification) —
  VAO と VBO の関係(要点3)
- 復習として **Day03.md の要点(エッジ関数)、Day04.md(バリセントリック補間)、Day06.md(MVP と透視除算)**。
  今日はこの3日ぶんが「GPU 側では何になったか」として回収される

## 理論の要点

### 1. Phase 1 で書いたものは、GPU のどこに行ったのか

**今日いちばん重要な表。** Phase 1 で自作したソフトウェアラスタライザの各段階が、
GPU パイプラインのどこに対応するかを並べる。

| Phase 1 で自分が書いたもの | GPU では | 触れるか |
|---|---|---|
| Day 6 頂点変換(MVP を掛ける) | **頂点シェーダ** | **書く** |
| Day 10 クリッピング(近クリップ面) | クリッピング | 固定機能 |
| Day 6 透視除算(w で割る) | 透視除算 | 固定機能 |
| Day 6 ビューポート変換 | ビューポート変換 | `glViewport` で範囲だけ指定 |
| Day 10 背面カリング | カリング | `glEnable(GL_CULL_FACE)` で ON/OFF |
| Day 3 エッジ関数・top-left rule | **ラスタライザ** | 固定機能。**一切触れない** |
| Day 4 バリセントリック補間 | 属性補間 | 固定機能(`out`/`in` に流すだけ) |
| Day 8 透視補正補間 | 同上 | 固定機能(既定で有効) |
| Day 8〜9 テクスチャとライティング | **フラグメントシェーダ** | **書く** |
| Day 7 Zバッファ | 深度テスト | `glEnable(GL_DEPTH_TEST)` |
| — | ブレンド | `glEnable(GL_BLEND)` |

**プログラマブルなのは両端だけ**で、真ん中は全部ハードウェアに焼き込まれている。
Day 3 のエッジ関数を最適化しようと Day 10 の改造課題で悩んだが、
GPU ではその部分に手を出す余地が最初から無い——代わりに、
数千の演算器で並列に実行される。

この表があると、**Phase 1 は遠回りではなく前提だった**ことが分かる。
「頂点シェーダで gl_Position に何を書けばいいのか」は、
Day 6 で自分でクリップ座標を計算した人にとっては自明の話でしかない。

### 2. シェーダは「1個につき1回」走る小さなプログラム

GPU のシェーダが CPU の関数と決定的に違うのは、**呼び出し回数と並列性**。

| | 実行される単位 | 今日の実行回数 |
|---|---|---|
| 頂点シェーダ | 頂点1個につき1回 | **3回** |
| フラグメントシェーダ | 塗るピクセル1個につき1回 | **十数万回** |

Day 9 でフォンシェーディングが重かったのは、まさにこの「十数万回」を
CPU が1コアで順番に回していたから(実測 +7ms)。GPU は同じ処理を
数千の演算器に配って同時に走らせる。

もう1つの違いが**コンパイルのタイミング**。GLSL のコンパイルは実行時で、
GPU ごとに命令セットが違うためドライバがその場で機械語に落とす。
つまり**シェーダの文法エラーは実行時にしか出ない**。
Day 12 で `AllocConsole` を仕込んでおいたのは、このログを読むため。

実際にセミコロンを1つ忘れると、こう出る(本Dayの環境で確認):

```
フラグメントシェーダのコンパイルに失敗した:
0(7) : error C0000: syntax error, unexpected identifier, expecting ',' or ';' at token "undefinedFunction"
```

`0(7)` は「0番目のソースの7行目」。**行番号が出る**ので、
ログさえ読めればデバッグは C# と大差ない。逆に、ログを捨てる実装にしてしまうと
「何も出ない」だけが手掛かりという地獄になる。

### 3. VBO と VAO — 「データ」と「読み方」を分ける

初見でいちばん混乱するところなので、先に役割を分けておく。

- **VBO**(Vertex Buffer Object)= GPU 上の**ただのバイト列**。意味を持たない
- **VAO**(Vertex Array Object)= そのバイト列を**どう解釈するか**の記録

今日の頂点データはこう並んでいる。1頂点が5つの float で、**交互**に詰めてある。

```
[ x, y, r, g, b ][ x, y, r, g, b ][ x, y, r, g, b ]
  └─位置─┘└─色──┘
  ← stride = 20バイト →
```

これを「位置は先頭から2個」「色は8バイト目から3個」と教えるのが `glVertexAttribPointer`。

```csharp
GL.glVertexAttribPointer(0, 2, GL_FLOAT, GL_FALSE, stride, IntPtr.Zero);          // 位置
GL.glVertexAttribPointer(1, 3, GL_FLOAT, GL_FALSE, stride, new IntPtr(8));        // 色
```

第1引数の番号が、GLSL の `layout (location = 0)` と対応する。
**ここが食い違ってもコンパイルは通り、実行時に絵が壊れるだけ**なので気を付ける。

位置と色を別々の配列にする持ち方(SoA)もあるが、交互(AoS)にすると
1頂点ぶんが連続するので GPU のキャッシュに乗りやすい。実際のモデルデータもほぼこの形で、
Phase 1 の `Vertex` 構造体の配列と同じ発想。

VAO のおかげで、描画時は `glBindVertexArray` 1回で全部の設定が復元される。
VAO が無かった OpenGL 2.x の時代は、描画のたびに全属性の設定を呼び直していた。
**コアプロファイルでは VAO は省略できない**(0番のままだと描画がエラーになる)。

細かいが引っかかりやすい点を1つ。**`GL_ARRAY_BUFFER` のバインドは VAO の状態に含まれない**が、
**インデックスバッファ(`GL_ELEMENT_ARRAY_BUFFER`)は含まれる**。
改造課題2でインデックス描画をやると、ここを踏む。

### 4. 頂点属性・uniform・補間 — 3種類のデータの流れ

シェーダにデータが入る経路は2つ、出る経路は1つ。

| 種類 | 変わる単位 | 今日の例 |
|---|---|---|
| **頂点属性**(`in`) | 頂点ごと | 位置、色 |
| **uniform** | ドローコールごと(全頂点で同じ) | 変換行列 |
| **`out` → `in`** | ラスタライザが**補間**して渡す | 色 |

3つ目が Day 4 の回収になる。頂点シェーダで

```glsl
out vec3 vColor;   // 頂点ごとの色を出す
```

と書き、フラグメントシェーダで

```glsl
in vec3 vColor;    // 補間済みの色が届く
```

と受けるだけで、間の補間は GPU がやる。**Day 4 で自分で書いたバリセントリック座標の計算が、
`out` と `in` の名前を揃えるだけになった**のが今日の一番の実感どころ。
しかも Day 8 で苦労した透視補正まで既定で入っている。

uniform について1つ注意。`glGetUniformLocation` が **-1 を返してもエラーとは限らない**。
「そんな名前は無い」(タイプミス)のほかに、
「宣言はしたが結果に影響しないので最適化で削除された」場合も -1 になる。
シェーダを書き換えている最中によく出るので、警告を出すようにしてある。

### 5. 列優先(column-major)— 行列で最初に踏む地雷

`glUniformMatrix4fv` に渡す float[16] は、**列優先**で並んでいることが期待される。

```
行列                    配列の並び(列優先)
| m0  m4  m8  m12 |     [m0, m1, m2, m3,  m4, m5, m6, m7,  ...]
| m1  m5  m9  m13 |      └── 第0列 ──┘  └── 第1列 ──┘
| m2  m6  m10 m14 |
| m3  m7  m11 m15 |
```

つまり配列の最初の4要素が**1行目ではなく1列目**。
`m[列 * 4 + 行]` の順で詰める。

行優先で持っている場合は第3引数の `transpose` を `GL_TRUE` にすればドライバが転置してくれるので、
**どちらでもよい。ただし片方に決めて一貫させること。** 混ざると、
「回転しているように見えるが向きが逆」「平行移動が効かない」といった
原因の分かりにくい症状になる。

なお平行移動成分が `m12, m13, m14` に入るのは列優先での話。
Phase 1 の自作 `Mat4` がどちらだったかを確認しておくと、Phase 3 で移植するときに困らない。

### 6. GLboolean は1バイト — マーシャリングの罠

`glVertexAttribPointer` の `normalized` と `glUniformMatrix4fv` の `transpose` は
`GLboolean` 型で、これは **C の `unsigned char`、つまり1バイト**。

ところが C# の `bool` を P/Invoke でマーシャリングすると、既定では
**4バイトの Win32 BOOL** になる(Day 11 の要点で「Win32 では既定が正しい」と書いたのと逆)。
そのまま渡すと引数のバイト数が合わず、後続の引数がずれる。

このバインディングでは `GLboolean` を取る引数をすべて `byte` にして、
`GL_FALSE = 0` / `GL_TRUE = 1` を定数で用意した。
**型が同じ名前でもサイズが違う**——P/Invoke で最も静かに壊れる類の問題で、
`GLsizeiptr` を `int` で受けてしまう事故(64bit でずれる)も同じ系統。

### 7. ドローコール1行の向こう側

```csharp
GL.glDrawArrays(GL.GL_TRIANGLES, 0, 3);
```

CPU が送るのはこの1行だけ。この向こうで、頂点シェーダが3回、
クリッピングと透視除算とビューポート変換、ラスタライズ、
そしてフラグメントシェーダが十数万回走る。

**実測**(本Dayの環境、640x480):

| 設定 | fps |
|---|---|
| VSync ON | 60.0 |
| VSync OFF | **約 5,660** |

Day 12 の「画面をクリアするだけ」が 5,743 fps だったので、
**三角形を1枚描いても fps はほとんど変わらない**。
GPU にとって三角形1枚は誤差でしかなく、この数字はほぼ
「ループと SwapBuffers のオーバーヘッド」を測っている。

Phase 1 の Day 10 は、三角形 2,202枚で 17.8ms(約41fps)だった。
GPU なら同じ絵が桁違いに軽い——それを確かめるのは Phase 3 の仕事になる。

ここで「ドローコールを減らせ」という定番の助言の意味も見えてくる。
重いのは三角形ではなく、**CPU から GPU へ命令を送ること自体**。
Day 17〜18 のスプライトバッチ(1万枚を1ドローコール群で)は、まさにこの話。

## Phase 2 の総括 — 3日間で剥がした層

| Day | 自分で書いたもの | Silk.NET なら |
|---|---|---|
| 11 | ウィンドウクラス登録、ウィンドウプロシージャ、メッセージループ、DPI | `Window.Create()` |
| 12 | ピクセルフォーマット、wgl コンテキスト、関数の動的ロード、VSync | 同上(内部で実施) |
| 13 | シェーダのコンパイル/リンク、VAO/VBO、ドローコール | `GL` オブジェクト経由で同じことを書く |

Day 11〜12 は**丸ごと1行に消える**。Day 13 で書いた内容は Phase 3 でも書き続けるが、
関数の宣言は Silk.NET が生成済みのものを使う。

つまり Phase 2 で捨てるのは「儀式」だけで、**GPU の使い方そのものは残る**。
ロードマップが「一度やれば十分な領域」と書いたのはこの切り分けのこと。
Day 14 から Silk.NET を使うときに、それが何を代行しているかを知った状態で使える。

## 前Dayからの差分概要

| ファイル | 変更 | 内容 |
|---|---|---|
| `Shader.cs` | **新規(186行)** | シェーダのコンパイル・リンク・エラーログ・uniform |
| `GL.cs` | **+199行** | シェーダ、VAO/VBO、描画の関数27個と定数 |
| `Program.cs` | +244 / -92行 | 三角形のセットアップと描画。シェーダのソースも含む |
| `Win32.cs` | **変更なし** | |
| `Wgl.cs` | **変更なし** | |
| `GLContext.cs` | **変更なし** | |
| `Win32Window.cs` | **変更なし** | Day 11 から3日間まったく触っていない |

**Day 11〜12 で書いた4ファイルが1行も変わっていない**のが今日の構造上の見どころ。
ウィンドウの生成・コンテキストの生成・OpenGL の使用が、きれいに分離できている。

実コード行の増分は約300行。うち `GL.cs` の119行は関数宣言なので、
**考えるのは `Shader.cs` の104行と `Program.cs` の三角形まわり**。1〜2時間で収まる。

### 写経する順番

1. `GL.cs` の差分 — 定数 → デリゲート → フィールド → `Load()` の4箇所に追記。単調
   (`GLboolean` を byte にしている理由=要点6 だけ押さえる)
2. `Shader.cs` — コンパイル → リンク → ログ取得。
   「必要な長さを聞いてから取りに行く」2段構えの呼び方に慣れる
3. `Program.cs` のシェーダソース2本 — GLSL は短い。`layout(location)` と `out`/`in` の対応を確認
4. `Program.cs` の `CreateTriangle` — **今日の中心**。VAO → VBO → 属性、の順序が重要
5. `Program.cs` の `RunLoop` と `FillTransform` — 描画は5行。列優先(要点5)に注意

差分の確認:

```
git diff --no-index reference/Day12/GL.cs      reference/Day13/GL.cs
git diff --no-index reference/Day12/Program.cs reference/Day13/Program.cs
```

## 完成条件

```
dotnet run --project reference/Day13 -c Release
```

1. コンソールに `GL_RENDERER` と **`シェーダのコンパイルとリンクに成功`** が出る
2. **三角形が1枚、ゆっくり回転している**。1秒で約57度(1ラジアン)
3. **頂点の色が滑らかに混ざっている**。上が赤、左下が緑、右下が青で、
   中心はそれらの中間色。**境界がはっきりした3色に分かれていたら補間が効いていない**
4. 回転しても**三角形が歪まない**(正三角形に近い形を保つ)。
   歪むならアスペクト補正が効いていない(要点5)
5. **W キーでワイヤーフレーム**になる。輪郭線だけになり、内部の色の混ざりが消える
6. **V キーで VSync OFF** にすると fps が数千に跳ね上がる(本Dayの環境で約5,660)。
   Day 12 の「クリアのみ」とほぼ同じ数字であることを確認する
7. **Space** で回転が止まり、もう一度押すと止まった角度から再開する
8. **Esc** / **[×]** / **Alt+F4** で終了コード 0 で終了する

参考として、本Dayの環境で `glReadPixels` を使って実際の描画結果を読み出した値
(クリア色は 26,28,33):

| 位置 | R | G | B | |
|---|---|---|---|---|
| 左下すみ (5,5) | 25 | 28 | 33 | 三角形の外なのでクリア色 |
| 上の頂点付近 (320,370) | 245 | 56 | 57 | ほぼ赤 |
| 左下の頂点付近 (200,130) | 59 | 234 | 64 | ほぼ緑 |
| 右下の頂点付近 (440,130) | 59 | 64 | 234 | ほぼ青 |
| 中央 (320,240) | 144 | 106 | 107 | **3色が混ざっている** |

うまくいかないときの確認ポイント:

- **何も出ない(クリア色だけ)** → 原因が多いので順に潰す。
  1. `glBindVertexArray` を描画前に呼んでいるか
  2. `glEnableVertexAttribArray` を忘れていないか(**属性は既定で無効**)
  3. `shader.Use()` を呼んでいるか
  4. `uTransform` が全部0の行列になっていないか(初期化忘れ)
- **真っ白 / 真っ黒な三角形が出る** → 色の属性が届いていない。
  `glVertexAttribPointer` の第1引数(1番)と GLSL の `layout(location = 1)` の対応、
  およびオフセット(8バイト)を確認
- **三角形が画面外へ飛ぶ / 極端に大きい** → 行列の並びが行優先になっている(要点5)。
  `transpose` を `GL_TRUE` にすると直るなら、それが原因
- **横に間延びした二等辺三角形になる** → アスペクト補正が入っていない。
  これは**バグではなく NDC の仕様**で、Phase 1 の投影行列と同じ話(要点5)
- **`シェーダのコンパイルに失敗した`** → ログの行番号を読む。
  よくあるのは `#version 330 core` の書き忘れと、
  コアプロファイルで廃止された `gl_FragColor` の使用
- **GLSL のコメントの日本語が原因で落ちる** → ソースの渡し方。
  自動マーシャリング(ANSI)ではなく UTF-8 で渡しているか(`Shader.CompileShader`)
- **`uniform 'uTransform' が見つからない` の警告** → シェーダ内で実際に使われていないと
  最適化で消える。`gl_Position` の計算に使っているか確認(要点4)
- **描画が1フレーム目だけおかしい** → `glViewport` を呼ぶ前に描いている

## 改造課題

### 課題1(易): シェーダを書き換えて遊ぶ

**シェーダは実行時コンパイルなので、書き換え→実行のサイクルが速い。** まずは慣れる。

1. フラグメントシェーダで色を反転する(`FragColor = vec4(1.0 - vColor, 1.0)`)
2. 頂点シェーダで、頂点を法線方向ではなく**中心から外へ膨らませる**
   (`gl_Position` に渡す前に `aPosition * 1.5` など)
3. **わざと壊す。** セミコロンを消す、存在しない関数を呼ぶ、`out` の名前を片方だけ変える。
   それぞれどんなエラーメッセージになるか。**リンクエラーとコンパイルエラーの違い**は出るか
4. W キーでワイヤーフレームにしたとき、**なぜ色の混ざりが見えなくなるのか**を説明できるか
   (ヒント: フラグメントシェーダが走るピクセルがどこか)

### 課題2(中): インデックスバッファで四角形を描く

三角形2枚で四角形を作る。素直にやると頂点が6個必要だが、実際には4個で足りる。

1. 頂点を4個(四隅)にして、`[0,1,2, 2,3,0]` というインデックス配列を用意する
2. `GL_ELEMENT_ARRAY_BUFFER` に EBO を作り、`glDrawElements` で描く
   (`glDrawElements(GLenum mode, GLsizei count, GLenum type, const void *indices)`。
   type は `GL_UNSIGNED_INT`)
3. **VAO のバインドを外す順序を変えて、壊してみる。**
   要点3で触れたとおり EBO のバインドは VAO に含まれるので、
   VAO をバインドしたまま `glBindBuffer(GL_ELEMENT_ARRAY_BUFFER, 0)` を呼ぶと
   描画されなくなる。実際に踏んで、なぜかを説明できるか
4. 頂点数とインデックス数を比べる。立方体(8頂点・36インデックス)ならどうか。
   Day 9 で索引付きメッシュにしたときの理屈がそのまま GPU 側に来ている

### 課題3(難): シェーダのホットリロード

**実務で最も効く仕組み。** シェーダを書き換えるたびにアプリを再起動するのは、
1日に何十回もやると効率が段違いに落ちる。

1. シェーダのソースを C# の定数から **`.vert` / `.frag` のファイルへ出す**
   (パス解決は Phase 1 の `ResolveAssetPath` が参考になる)
2. **F5 キーで再読み込み**して、シェーダプログラムを作り直す
3. **コンパイルに失敗しても落ちないようにする。** これが肝で、
   失敗したらエラーをコンソールに出して**古いシェーダを使い続ける**。
   落ちると結局再起動になってしまい、意味が半減する
4. 発展: `FileSystemWatcher` でファイルの更新を検知し、
   保存した瞬間に反映されるようにする。
   ただし**変更通知は複数回飛ぶ**ことと、**保存途中の不完全なファイルを読む**ことがあるので、
   少し待ってから読むなどの対策が要る
5. 古いシェーダプログラムの `glDeleteProgram` を忘れないこと。
   何十回もリロードすると GPU リソースが積み上がる

## 動作確認済み環境

- .NET SDK 10.0.102 / Windows 11 / NVIDIA GeForce RTX 3070(ドライバ 596.49)
- OpenGL 3.3 コアプロファイル / GLSL 3.30
- VSync ON で 60.0 fps、OFF で約 5,660 fps
  (Day 12 のクリアのみ 5,743 fps とほぼ同じ = 三角形1枚のコストは誤差)
- `glReadPixels` で描画結果を実測し、頂点色の補間が効いていることを確認(完成条件の表)
- GLSL ソース内の日本語コメントが UTF-8 で正しくコンパイルされることを確認
- シェーダのコンパイルエラーが行番号付きで取得できることを確認
- W(ワイヤーフレーム)/ V(VSync)/ Space(一時停止)/ Esc の各キーを実測で確認
- `dotnet build` 警告 0 / `dotnet format whitespace --verify-no-changes` 差分 0
