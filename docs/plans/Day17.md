# Day 17: 2Dスプライトバッチ(動的頂点バッファ)

Phase 3 / ロードマップ該当行: 「2Dスプライトバッチ(動的頂点バッファ)」

## 今日のゴール

**Day 16 の3Dシーンの上を、1万枚のスプライトが飛び回っても 60fps を割らない。**

ここまでの `Mesh<TVertex>` は「1つの形 = 1回の `glDrawElements`」だった。
その調子でスプライトを1万枚描くと、**1万回のドローコール**になる。
実測すると、それだけで1フレーム 38.6ms かかる(60fps の予算は 16.6ms)。

今日は同じ1万枚を**4回のドローコール**で描く。0.6ms になる。
GPU の仕事量は変わっていない。減らしたのは「描けと命じる回数」だけ。

## 事前に読む資料

ロードマップの指定は「-」(特論に対応する回が無い)。実装寄りの回なので、
次を読んでおくと引っかかりが減る。

- [Buffer Object Streaming — OpenGL Wiki](https://www.khronos.org/opengl/wiki/Buffer_Object_Streaming) —
  毎フレーム書き換えるバッファの扱い。**オーファニング**もここ
- [Blending — LearnOpenGL](https://learnopengl.com/Advanced-OpenGL/Blending) —
  アルファブレンドと、半透明を描く順の話
- [Vertex Specification Best Practices — OpenGL Wiki](https://www.khronos.org/opengl/wiki/Vertex_Specification_Best_Practices) —
  頂点フォーマットをどう決めるか
- 復習として **Day13.md の要点3(VAO/VBO/EBO)** と **Day15.md の要点7(頂点構造体)**。
  今日はそこに「毎フレーム作り直す」が加わるだけ

## 理論の要点

### 1. ドローコールは1回いくらか

「ドローコールは高い」とよく言われるが、いくらなのかを測っておくと判断が早くなる。
このリポジトリのスプライト(テクスチャ1枚、uniform 変更なし)で測ると:

| 条件 | ドローコール | fps | ms/frame |
|---|---|---|---|
| 1000枚 バッチあり | 2 | 1628 | 0.614 |
| 1000枚 バッチなし | 1000 | 258 | 3.879 |
| 10000枚 バッチあり | 4 | 1653 | 0.605 |
| 10000枚 バッチなし | 10000 | 25.9 | 38.629 |
| 20000枚 バッチあり | 6 | 948 | 1.055 |

バッチなしの2行から、**ドローコール1回あたり約 3.9 マイクロ秒**と読める
(3.879ms / 1000 も 38.629ms / 10000 も同じ 3.9µs になる。きれいに線形)。

この数字が分かると、予算が計算できる。60fps の 16.6ms を**全部**ドローコールに
費やしても **4200回** が上限。実際には他の処理もあるので、現実的な目安は
1フレーム 1000〜2000 回。「1万個のオブジェクトを個別に描く」は最初から成立しない。

そして高いのは **CPU 側**であることに注意。GPU は 10000 回でも 4 回でも
同じ量の三角形を塗っているだけで、暇をしている。
**GPU を遊ばせないために CPU の口数を減らす**のがバッチング。

### 2. バッチの原理 — 「1万個の絵」を「1個の巨大な絵」にする

やることは単純で、**1万枚ぶんの頂点(4万頂点)を1本のバッファに詰めて、
1回で描かせる**。GPU から見れば4万頂点の三角形リストでしかなく、
それが1万個の別々の絵だとは知らない。

これを成立させるには条件がある。**オブジェクトごとの uniform を持てない**こと。

Day 16 の 3D 描画は、オブジェクトごとに `uModel` を送っていた。
これをスプライトでやると、1万回 `glUniformMatrix4fv` を呼ぶことになり、
ドローコールを減らした意味が消える。

だからスプライトバッチは、**位置・回転・大きさを CPU 側で頂点に焼き込む**。
`SpriteBatch.Draw` が回転行列を掛けた4隅の座標を計算して配列に書く。
シェーダに渡る uniform は、フレームに1本の投影行列だけになる。

```
3D  : 頂点は固定、行列で動かす        → オブジェクトごとに uniform が要る
2D  : 頂点そのものを毎フレーム作り直す → uniform はフレームに1本
```

「毎フレーム4万頂点ぶんの座標を CPU で計算するのは無駄では?」と思うところだが、
そのコストは実測 0.6ms の中に収まっている。**3.9µs × 1万回のほうが桁違いに高い**。

### 3. 動的頂点バッファ

`Mesh<TVertex>` を使い回せないのは、あちらが `StaticDraw` で
「1回アップロードして、あとは読むだけ」を前提にしているから。

スプライトバッチのバッファの扱いは3点で違う。

**(a) 確保と書き込みを分ける**

```csharp
// 生成時: サイズだけ確保。中身は未定義(第3引数が null)
gl.BufferData(ArrayBuffer, capacity * 4 * sizeof(SpriteVertex), null, BufferUsageARB.StreamDraw);

// 毎フレーム: 使ったぶんだけ上書き
gl.BufferSubData(ArrayBuffer, 0, vertexCount * sizeof(SpriteVertex), data);
```

`StreamDraw` は「毎フレーム書き換えて、数回描いて捨てる」という**使い方の申告**。
`StaticDraw` / `DynamicDraw` / `StreamDraw` はドライバへのヒントでしかなく、
間違えても動く。ただしドライバはこれを見てメモリの置き場所を決めるので、
嘘をつくと遅くなることがある。

**(b) インデックスバッファは静的でよい**

四角形 i の頂点は必ず `4i, 4i+1, 4i+2, 4i+3` に並ぶので、
インデックスの中身は**スプライトの内容に一切依存しない**。
だから生成時に `capacity * 6` 個を1回作れば、あとは触らない。
**動的なのは頂点だけ**、というのが見落としやすいところ。

**(c) オーファニング — 定番と言われているが、測ったら効かなかった**

同じバッファに `BufferSubData` で上書きするとき、GPU がまだ前フレームの描画で
そのバッファを読んでいると、ドライバは読み終わるのを待つ(CPU が止まる)。
そこで同じサイズ・同じ用途で `BufferData` を呼び直すと、
「前の中身はもう要らない」という宣言になり、ドライバは古い領域を GPU に
使わせたまま**新しい領域をこちらに渡す**。古い領域は描き終わったら回収される
(= 孤児 orphan にする)。動的バッファの定番テクニックとされている。

このデモで測った結果:

| 条件 | ドローコール | fps | ms/frame |
|---|---|---|---|
| 20000枚 オーファニング ON | 6 | 944.5 | 1.059 |
| 20000枚 オーファニング OFF | 6 | 947.7 | 1.055 |

**差は 0.3% 以内**。1000枚では ON のほうがむしろ遅かった。
理由は単純で、**待つほど GPU が遅れていないから**。
オーファニングは「待ちを消す」技であって、待ちが無いところでは
`BufferData` の呼び出しぶんだけ損をする。効くのは GPU 律速のとき。

なので既定は OFF にして、O キーで切り替えられるようにしてある。
Day 2(Bresenham)・Day 9(シェーディング)で繰り返し踏んだのと同じ話で、
**定番と呼ばれる手法でも、自分の負荷で測るまでは分からない**。

### 4. フラッシュの条件 — バッチが「効かなくなる」のはいつか

`SpriteBatch` が溜めたぶんを吐き出す(= ドローコールを1回発行する)のは2つの場合。

1. **テクスチャが変わる** … シェーダに刺さっているテクスチャは1つだけなので、
   違う絵を同じドローコールに混ぜられない
2. **バッファが満杯** … 物理的に入らない

1 が今日いちばん大事なところ。**バッチは「連続する描画が同じ状態を使っている」
ときにしか効かない**。テクスチャを交互に使うと、こうなる:

| 条件 | ドローコール | fps |
|---|---|---|
| 1000枚 テクスチャごとにまとめる | 2 | 1628 |
| 1000枚 交互に描く | **1000** | 255 |

バッチは有効なのに、バッチなし(258 fps)とほぼ同じ。
**まとめる仕組みがあるだけでは足りず、まとめられる順に描く必要がある**。

今日のデモは呼び出し側が手で分けている(偶数番 → 奇数番)が、
本来これはバッチの中でやるべき仕事。それが Day 18 の**ソート**で、
そもそもテクスチャを1枚にまとめてしまうのが**アトラス**。
今日はこの問題を T キーで体感するところまで。

2 のほうは容量とのトレードオフ。容量 4000 なら 2万枚で 6 回のドローコール
(円 1万枚で3回 + リング 1万枚で3回)。容量を上げればドローコールは減るが、
頂点バッファのメモリが増える(4000枚 = 4000 × 4頂点 × 32バイト = 512KB)。
**ドローコール 3.9µs は、数回の差なら誤差**なので、無理に上げる意味は薄い。

### 5. 2D を描くための状態

同じフレームの中で 3D の次に 2D を描くと、**GL の状態を切り替える**必要がある。
Day 16 のままでは3つとも壊れる。

| 状態 | 3D では | 2D では | 忘れるとどうなるか |
|---|---|---|---|
| 深度テスト | ON | **OFF** | あとから描いた手前のスプライトが弾かれる |
| 背面カリング | ON | **OFF** | Y 下向きの座標系で巻きが裏返り、**全部消える** |
| ブレンド | OFF | **ON** | α が 0 の部分も塗り潰され、四角い板になる |

カリングを切る理由は、スクリーン座標が Y 下向きなこと。
投影行列で Y を反転している(要点6)ので、素直に「左上 → 右上 → 右下 → 左下」と
並べた四角形は NDC 上で時計回りになり、裏面と判定される。
回転で符号が変わる可能性もあるので、**2D では巻きを気にせずカリングを切る**のが普通。

`SpriteBatch.Begin` がこの3つを設定し、`End` で元に戻している。
ただし `BlendFunc` は戻していない。「何を保存して何を保存しないか」を
クラスごとに決め打ちするのは長期的に破綻するので、本来は
レンダーステートをまとめて持つ仕組みが要る。今日は
「`Begin` が必ず設定し直す」ことで辻褄を合わせてある。

アルファブレンドの式は:

```
結果 = 描く色 × α + すでにある色 × (1 - α)
```

`glBlendFunc(GL_SRC_ALPHA, GL_ONE_MINUS_SRC_ALPHA)` がこれ。
第2引数を `GL_ONE` にすると**加算ブレンド**になり、重なるほど明るくなる
(炎や魔法エフェクトの定番。改造課題1で試す)。

### 6. スクリーン座標をそのまま使う投影行列

2D では「左上が (0,0)、右下が (幅, 高さ)」でスプライトを置けたほうが圧倒的に楽。
そのための行列を `Camera.CreateScreen` に足した。呼び方は:

```csharp
Camera.CreateScreen(left: 0, right: width, bottom: height, top: 0, near: -1, far: 1);
```

**bottom に height、top に 0 を渡す**のがポイントで、これで Y 軸が下向きになる。
上下の入れ替えは M22 の符号1つで済むので、専用の反転行列は要らない。

そして **V 座標の反転が今日で3回目**になる。

| Day | 場面 | 対応 |
|---|---|---|
| 10 | OBJ の V が下から上 | 読み込み時に `1 - v` |
| 15 | 画像は上から、テクスチャは下から | `stbi_set_flip_vertically_on_load` |
| 17 | スクリーンは Y 下向き、テクスチャは V 上向き | 四隅への UV 割り当てで入れ替え |

3回とも「原点の取り方が違う2つの世界をつなぐ」という同じ問題で、
直し方も毎回1か所。**どこで1回だけ反転させるかを決めておく**のが肝で、
2か所で反転すると元に戻ってしまい、しかも動いてしまうので気付きにくい。

### 7. 今日やっていないこと(= Day 18 でやること)

今日の `SpriteBatch` には、まだ次の穴がある。全部 Day 18 で埋める。

- **テクスチャを混ぜられない** … 呼び出し側が順番を工夫するしかない → アトラス
- **描画順を制御できない** … 積んだ順にそのまま描く。半透明の前後関係を
  自分で解決できない → ソート
- **頂点が 32 バイトある** … 色を byte 4個にすれば 20 バイト。
  2万枚 = 8万頂点なので、毎フレーム 2.5MB → 1.6MB の差になる
- **テクスチャの一部を切り出す指定が UV** … スプライトシートはピクセルで
  指定したい

## 前Dayからの差分概要

### 新規ファイル

| ファイル | 行数 | 役割 |
|---|---|---|
| `SpriteBatch.cs` | 383 | 今日の主役。動的頂点バッファ、フラッシュ判定、2D 用の状態切り替え |
| `SpriteVertex.cs` | 45 | スプライト用の頂点(位置2 + UV2 + 色4 = 32バイト) |
| `shaders/sprite.vert` | 25 | 投影行列を掛けるだけ。**モデル行列が無い**のが要点 |
| `shaders/sprite.frag` | 15 | テクスチャ × 頂点色(α も掛ける) |

### 変更ファイル

| ファイル | 差分 | 内容 |
|---|---|---|
| `Program.cs` | +265 / -84 | スプライトの生成・更新・描画、B/T/O/3/上下キー |
| `Camera.cs` | +29 / -0 | `CreateScreen`(スクリーン座標用の平行投影)を追加 |

`Mesh.cs` / `Material.cs` / `Texture.cs` / `Shader.cs` / `Vertex.cs` /
`Primitives.cs` / `OrbitCameraController.cs` / `shaders/textured.*` は**すべて無変更**。
実コード行数(コメント・空行を除く)は 441 行。

### 追加した素材

| ファイル | 内容 |
|---|---|
| `assets/textures/sprite-circle.png` | 64x64 RGBA。縁をぼかした円。中心がわずかに明るい |
| `assets/textures/sprite-ring.png` | 64x64 RGBA。中央が抜けた輪 |

どちらも外部からのダウンロードではなく、Python の `zlib` だけで書いた
PNG エンコーダで生成した(Day 15 の `uv-test.png` と同じやり方)。
白〜灰色にしてあるので、頂点色を掛けるだけで好きな色になる。
**アルファ付き**なのがここまでのテクスチャとの違いで、
ブレンドを有効にしないと四角い板として描かれる。

### 写経する順番

1. **`SpriteVertex.cs`** — 短い。なぜ `Vertex` と分けるのかだけ押さえる
2. **`shaders/sprite.vert` / `sprite.frag`** — 合わせて40行。
   **モデル行列が無い**ことの意味(要点2)を確認してから書く
3. **`Camera.CreateScreen`** — 12行。Y 反転の仕組み
4. **`SpriteBatch.cs`** — 今日の本体。上から順に
   コンストラクタ(バッファ確保) → `Begin`(状態) → `Draw`(頂点を積む) →
   `Flush`(送って描く) → `End`(状態を戻す)
5. **`Program.cs`** — スプライトの配列と更新は普通のループ。
   見るべきは `RenderSprites` の「まとめる/まとめない」の分岐だけ

## 完成条件

`dotnet run --project reference/Day17 -c Release` で確認する。
**必ず `-c Release`**。今日は数字を見る回なので、Debug では意味が変わる。

### 見た目

- Day 16 の3Dシーン(市松の床と6個の立方体)の**手前に**、
  円と輪のスプライトが1000枚飛び回っている
- スプライトは半透明で、**重なると下の絵が透ける**。輪の中央は抜けている
- 一枚ずつ違う色と大きさで、自転しながら画面の端で跳ね返る
- 左ドラッグでカメラを回すと、**3Dシーンだけが動いてスプライトは動かない**
  (スプライトはスクリーン座標なので、カメラの影響を受けない)

### 数字(タイトルバー)

fps の絶対値はウィンドウが見えているか(隠れていると SwapBuffers が待たない)で
数倍変わるので、**ドローコール数と、切り替え前後の比**を見る。

| 操作 | ドローコール | fps |
|---|---|---|
| 起動直後(1000枚) | **2** | 基準値 |
| **B**(バッチ OFF) | **1000** | 基準の **1/6 以下**に落ちる |
| **T**(まとめ OFF) | **1000** | B を切ったときとほぼ同じ |
| **上キー×9**(10000枚) | **4** | 1000枚とほとんど変わらない |
| 10000枚 + **B** | **10000** | **1/60 以下**。ここが今日の山場 |
| **上キー**(20000枚) | **6** | 10000枚の 6 割程度 |
| **O**(オーファニング ON) | 変化なし | **ほとんど変わらない**(要点3) |
| **3** | 変化なし | 3D背景が消え、スプライトだけの負荷が測れる |
| **V** | 変化なし | VSync ON。60fps に張り付く |

**10000枚でバッチを切ったとき**が今日いちばん見るべきところ。
計測環境(下記)では 1653 fps が **25.9 fps** まで落ちた。
これが「1万枚を個別に描く」の現実で、`SpriteBatch` はドローコールを
10000 回から 4 回に減らすだけでこれを取り戻す。

Day 16 までのキー(Z / C / P / W / F / R / Space / Home / F5 / Esc)もそのまま使える。

### ビルド

- `dotnet build reference/Day17` が警告 0 で通る
- `dotnet format whitespace reference/Day17/Day17.csproj --verify-no-changes` の差分が 0

## 改造課題

### 課題1(易): ブレンドで遊ぶ

要点5のブレンド式を実際にいじる。**状態を戻し忘れるバグ**も踏んでおく。

1. `SpriteBatch.Begin` の `BlendFunc` を
   `(BlendingFactor.SrcAlpha, BlendingFactor.One)` に変える。**加算ブレンド**になり、
   重なるほど明るくなる。炎・魔法・パーティクルの定番
2. 加算ブレンドのまま、スプライトの色のアルファを下げる(0.85 → 0.2)。
   「濃さ」の意味が通常ブレンドとどう違うか
3. `SpriteBatch.End` から `SetCap(EnableCap.Blend, _savedBlend)` を消してみる。
   **次のフレームの3D描画がどうなるか**。原因に辿り着けるか
4. 3 の状態で、さらに `Begin` の `_gl.Enable(EnableCap.Blend)` も消す。
   1フレーム目だけ正しく、2フレーム目から壊れる…といった挙動になるはず。
   **状態を持つ API のいやらしさ**を1回体験しておくと、Day 18 以降で助かる

### 課題2(中): 容量とドローコールのトレードオフを測る

要点4の後半を数字にする。

1. `SpriteBatch` の `capacity` を 100 / 500 / 4000 / 20000 に変え、
   スプライト2万枚でのドローコール数と fps を記録する
2. ドローコール数は予想どおりに変わるはず。**fps はどうか**。
   3.9µs × 何回ぶんの差になっているか
3. 頂点バッファのメモリ量も計算して並べる
   (capacity × 4頂点 × 32バイト)。どこが妥当か
4. インデックスを `uint` から `ushort` に変える。
   インデックスバッファが半分になるが、**capacity の上限が 16384 になる**。
   なぜか(ヒント: 頂点番号は `capacity × 4`)
5. 発展: `capacity` を超える枚数を1つのテクスチャで描いたとき、
   フラッシュが**フレームの途中で**起きる。半透明の描画順に影響が出ないのはなぜか
   (ヒント: 今日は深度テストを切っている)

### 課題3(難): インスタンシングで書き直して比べる

バッチングとは**別の解き方**がある。同じ形を何度も描くなら、
頂点を複製せずに「この頂点データを N 回描け」と1回で命じればよい。

1. 四角形1枚ぶん(4頂点・6インデックス)の**静的な**バッファを作る
2. スプライトごとの情報(位置・大きさ・回転・色)を別の VBO に入れ、
   `glVertexAttribDivisor(location, 1)` を指定する。
   divisor が 1 の属性は、**頂点ごとではなくインスタンスごとに1回進む**
3. `glDrawElementsInstanced(GL_TRIANGLES, 6, GL_UNSIGNED_INT, 0, spriteCount)` で描く
4. 頂点シェーダで、四角形のローカル座標にインスタンスの回転・拡大・平行移動を適用する。
   **CPU でやっていた4隅の計算が GPU に移る**
5. 転送量を比べる。バッチングは1枚 **128 バイト**(4頂点 × 32)、
   インスタンシングは1枚 **24 バイト**(位置8 + サイズ8 + 回転4 + 色4[byte×4])。
   **5分の1以下**になる
6. 両方で2万枚を測る。どちらが速いか。**予想と合ったか**

6 が本題。転送量が4分の1になっても速くならない可能性が高い
(要点1で見たとおり、この規模ではもう転送がボトルネックではない)。
インスタンシングが本当に効いてくるのは、1インスタンスあたりの頂点数が多いとき
(草・木・瓦礫を数万本)で、それは Day 32 以降の話。
**「同じ問題に2つの解き方があり、どちらが良いかは規模で決まる」**を体験しておく。

## 動作確認済み環境

- .NET SDK 10.0.102 / Windows 11 / NVIDIA GeForce RTX 3070(ドライバ 596.49)
- Silk.NET 2.23.0 / StbImageSharp 2.30.16 / OpenGL 3.3 コアプロファイル
- 960x640 / VSync OFF / Release ビルド
- 計測は各条件 240 フレーム(最初の30フレームは除外)の平均:

  | 条件 | ドローコール | fps | ms/frame |
  |---|---|---|---|
  | 1000枚 バッチON まとめON | 2 | 1628.3 | 0.614 |
  | 1000枚 バッチOFF | 1000 | 257.8 | 3.879 |
  | 1000枚 バッチON まとめOFF(交互) | 1000 | 254.6 | 3.927 |
  | 10000枚 バッチON まとめON | 4 | 1652.9 | 0.605 |
  | 10000枚 バッチOFF | 10000 | 25.9 | 38.629 |
  | 20000枚 バッチON まとめON | 6 | 947.7 | 1.055 |
  | 20000枚 オーファニングON | 6 | 944.5 | 1.059 |

- ドローコール1回あたりのコストは 3.879ms/1000 も 38.629ms/10000 も
  **3.9µs** で一致(線形)
- 上の計測はウィンドウが見えている状態のもの。`reference/Day17` を最小化した状態で
  同じ1000枚を回すと 4400 fps 出た(SwapBuffers が待たなくなるため)。
  **絶対値は環境と窓の状態で数倍動く**ので、比較するときは条件をそろえる
- フレームバッファを読み出して画像化し、次を確認:
  - 3Dシーンの手前に1000枚のスプライトが乗り、**下の絵が透けている**
  - 輪の中央が抜けている(アルファが効いている)
  - 円の縁がぼけている(アルファのグラデーションが効いている)
- `dotnet build` 警告 0 / `dotnet format whitespace --verify-no-changes` 差分 0
