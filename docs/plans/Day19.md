# Day 19: ゲームループ(固定タイムステップ+補間)

Phase 4 / ロードマップ該当行: 「ゲームループ(固定タイムステップ+補間)」

## 今日のゴール

**シミュレーションを 5Hz まで落としても、絵が滑らかなまま動く。**

Day 18 までの更新はこう書いていた。

```csharp
sprite.Position += sprite.Velocity * (float)deltaSeconds;   // 可変タイムステップ
```

短くて分かりやすいが、これは**同じ操作をしても環境によって結果が変わる**書き方になっている。
今日それを「シミュレーションは固定間隔、描画は好きなだけ」に組み替える。

Phase 3 が「絵をどう出すか」だったのに対し、Phase 4 は**エンジンコア**——
描画以外の土台を作る。ゲームループはその最初の1本で、
このあとの入力(Day 20)・コンポーネント(Day 22)・ECS(Day 23)は
すべて「毎ステップ何が呼ばれるか」の上に載る。

## 事前に読む資料

- [Fix Your Timestep! (Gaffer On Games)](https://gafferongames.com/post/fix_your_timestep/) —
  ロードマップ指定。**今日の内容はほぼこの記事**。
  可変 → 固定 → アキュムレータ → 補間、と段階的に組み上げていく構成なので、
  写経の前に一度通しで読んでおくとよい
- [deWiTTERS Game Loop](https://dewitters.com/dewitters-gameloop/) —
  同じ話を別の切り口で。図が多い
- [Game Programming Patterns: Game Loop](https://gameprogrammingpatterns.com/game-loop.html) —
  Robert Nystrom。パターンとしての整理と、各方式の向き不向き
- 復習として **Day17.md の要点1(ドローコールの実測)**。
  今日も「予算 16.6ms をどう配るか」の話が出てくる

## 理論の要点

### 1. 可変タイムステップの何が問題か

「経過時間を掛ければ、fps が変わっても同じ速さで動く」——これは半分正しい。
等速直線運動なら確かに合う。合わなくなるのは次の3つ。

**(a) 再現しない**

同じ操作をしても、fps が違えば結果が変わる。
浮動小数の足し算は結合則を満たさないので、
`0.016 を60回` と `0.008 を120回` は**別の値**になる。
1回の誤差は無視できても、数千ステップ積み上がると軌道が変わる。

これが困るのは、リプレイ、タイムアタックの記録検証、
ロールバック方式のネットワーク対戦——**同じ入力から同じ結果を再現したい**場面すべて。

**(b) 壊れる**

フレームが長引くと1ステップの移動量が大きくなる。
弾が壁を「すり抜ける」典型的なバグはこれ。
物理を入れると、積分の刻みが粗くなって発散する
(バネがどんどん振幅を増して吹き飛ぶ、など。Day 43 で本格的に扱う)。

**(c) 調整が効かない**

「1秒で何回更新されるか」が環境任せなので、ゲームバランスの数値が確定しない。
「毎ステップ 0.5 ダメージ」は 60Hz なら毎秒 30、144Hz なら毎秒 72 になる。

### 2. アキュムレータ — 溜めて、固定間隔で吐き出す

対策はひとつで、**シミュレーションを固定間隔で回す**。
ただし描画のタイミングは固定間隔とは一致しないので、
経過時間をいったん溜めて、溜まったぶんだけ進める。

```csharp
_accumulator += frameSeconds;

while (_accumulator >= FixedDeltaTime)
{
    fixedUpdate(FixedDeltaTime);   // ← 常に同じ値
    _accumulator -= FixedDeltaTime;
}
```

1フレームでの呼び出し回数は 0 回のことも複数回のこともある。
描画が速ければ 0 が続き、遅ければまとめて回る。
**描画レートとシミュレーションレートが完全に分離する**のがこの数行の成果。

`fixedUpdate` に渡る値が毎回同じ、というのが決定性の実体になる。
今日の `Program.UpdateSprites` は
「引数だけで結果が決まる」形(グローバルな状態も乱数も時刻も読まない)に切り出してある。

### 3. 補間 — 5Hz のシミュレーションを 4000fps で滑らかに見せる

固定間隔にすると、そのままでは**シミュレーションのレートでしか絵が動かない**。
60Hz なら気付きにくいが、20Hz や 5Hz にすると一目でカクつく。

描画のタイミングは必ず「あるステップと次のステップの間」にある。
どのくらい進んでいるかが `Alpha = accumulator / FixedDeltaTime`(0.0〜1.0)なので、
前のステップと今のステップを混ぜれば「間」の絵が作れる。

```csharp
Vector2 drawn = Vector2.Lerp(sprite.PreviousPosition, sprite.Position, alpha);
```

代償は**状態を2つ持つこと**。動かす前に前の値を保存する必要があり、
これを忘れると前と後が同じ値になるので補間が効かない
(症状は「補間を入れたのにカクついたまま」)。

実測すると効果がはっきり出る。5Hz でスプライト1枚の
「1フレームあたりの画面上の移動量」を2秒間集計した結果:

| | 動かなかったフレームの割合 | 1フレーム最大移動 | 平均移動 |
|---|---|---|---|
| 補間 OFF | **99.9%** | **33.38 px** | 0.039 px |
| 補間 ON | **0.0%** | **0.20 px** | 0.042 px |

**平均移動がほぼ同じ**なのがポイント(差は計測窓の違いによる誤差)。総移動量は変わっておらず、
変わったのは**分配のしかた**だけ。補間 OFF は「200ms 止まって 33px 跳ぶ」、
補間 ON は「毎フレーム 0.04px ずつ動く」。同じ距離を進んでいる。

補間で気を付けるのは**瞬間移動**。ワープ、リスポーン、画面切り替えのときに
前と後を直線で結ぶと、画面を横切る線になってしまう。
そういう場面では前の状態を現在値で上書きして、補間を1フレーム無効にする。

### 4. 何を FixedUpdate に書き、何を描画に書くか

今日いちばん大事な設計判断。

| 置き場所 | 例 |
|---|---|
| **FixedUpdate** | 移動、当たり判定、AI、タイマー、ダメージ、入力の反映 |
| **描画** | 補間、カメラの追従、パーティクルの見た目、UI のアニメーション |

境界は「**ゲームの状態を変えるか**」。
状態を変える処理が描画側に紛れ込むと、その瞬間に決定性が壊れる。
「描画のついでにちょっと座標を直す」が最も入り込みやすい。

逆に、見た目だけのものを FixedUpdate に入れると、
シミュレーションレートを下げたときにそれまでカクつく。

### 5. 死のスパイラルと、遅れの扱い方

固定ステップには落とし穴がある。

```
フレームが重い → 溜まった時間が増える → 次フレームでステップ数が増える
→ もっと重い → もっと溜まる → …
```

これが**死のスパイラル(spiral of death)**で、一度入ると復帰できない。
1フレームで回すステップ数に上限(`MaxStepsPerFrame`)を付けるのが非常ブレーキ。

上限を付けると、遅い環境では**シミュレーションが実時間より遅れる**。
その遅れをどう扱うかが設計判断になる。

| 方針 | 挙動 | 使いどころ |
|---|---|---|
| **捨てる**(既定) | シミュレーションがスローモーションになる。遅れは溜まらない | ほとんどのゲーム |
| **捨てない** | 遅れが永久に増え続け、二度と追いつかない | ほぼ選ばない |

1ステップに 20ms かかる負荷をかけて実測した(60Hz なので1ステップの予算は 16.67ms。
つまり原理的に追いつけない):

| 設定 | fps | 遅れ | 捨てた時間(2秒間) |
|---|---|---|---|
| 捨てる | 41.0 | 4.6 ms | 0.20 s |
| 捨てない | **6.2** | **354.9 ms** | 0.00 s |

捨てる場合、シミュレーションは実時間の約 90% の速さで進む(2秒で 0.2 秒ぶん落とした)。
遅いが**動き続ける**。捨てない場合は毎フレームが上限いっぱいのステップ数になり、
fps が 6 まで落ちたうえで遅れが増え続ける。**非常ブレーキが無ければ完全に固まる**。

「捨てる」を選ぶのは、**重い環境ではゲーム内時間が実時間よりゆっくり進むのを許す**
という判断でもある。オンライン対戦のように実時間と同期しなければならない場合は、
捨てるのではなく描画やシミュレーションの質を落とすほうを選ぶ。

### 6. Silk.NET の固定レート機能をあえて使わない

`WindowOptions.UpdatesPerSecond` に値を入れると、Silk.NET が
`Update` イベントを固定レートで呼んでくれる。今日はこれを 0(上限なし)にして、
素のフレーム時間を自分の `GameLoop` に渡す。

出来合いを使わないのは、Phase 2 で wgl を自分で叩いたのと同じ理由——
**中で何が起きているかを一度見ておく**ため。
それに、出来合いのものには補間の仕組みが無い。`Alpha` を自分で持っていないと
要点3ができないので、どのみち書くことになる。

### 7. 決定性は測れる(Y キー)

「同じ入力なら同じ結果」は口で言うだけでは確かめられないので、
自己チェックを組み込んである。同じ初期状態から3通りに進めて突き合わせる。

- **A**: 固定ステップを 600 回、まとめて回す
- **B**: 同じ 600 回を、2〜32ms のばらばらなフレーム時間に分けて `GameLoop` 経由で回す
- **C**: 可変タイムステップで、同じ実時間ぶん進める

結果(状態の FNV-1a ハッシュ):

```
A 固定・まとめて      : 7F43863E1E5DFDB8
B 固定・フレーム分割  : 7F43863E1E5DFDB8  一致
C 可変・同じ実時間    : 7444383E9D7F0545  不一致
```

**A == B ≠ C**。フレームの刻み方が違っても、ステップ数が同じなら結果は同じ。
可変だと同じ実時間でも結果が変わる。

比較にビット単位のハッシュを使っているのは意図的で、
「誤差の範囲で合っている」では意味がないから。
1ビット違えば数千ステップ後には全く別の結果になる。

## 前Dayからの差分概要

### 新規ファイル

| ファイル | 行数 | 役割 |
|---|---|---|
| `GameLoop.cs` | 164 | アキュムレータ、`Alpha`、ステップ数の上限、遅れの扱い |

実コードは 48 行しかない。**今日の重さはコード量ではなく設計判断のほうにある**。

### 変更ファイル

| ファイル | 差分 | 内容 |
|---|---|---|
| `Program.cs` | +355 / -32 | `FixedUpdate` への分離、補間、決定性チェック、1/2/3/4・I・L・K・Y キー |

`Program.cs` 以外の Phase 3 のクラス(`SpriteBatch` / `Camera` / `Texture` /
`TextureAtlas` / `Mesh` / `Material` / `Shader` など)は**すべて無変更**。
レンダラ層とゲームループが独立していることの確認になっている。

内訳:

- `Sprite` に `PreviousPosition` / `PreviousRotation` を追加
- `OnUpdate` は `_loop.Advance(deltaSeconds, FixedUpdate)` を呼ぶだけになった
- `UpdateSprites` を「配列・個数・dt・画面サイズだけを受け取る」形に切り出した
  (決定性チェックから同じ関数を呼ぶため)
- `Interpolate` を通してから描画する
- `RunDeterminismCheck` と `Checksum`(要点7)
- **起動直後の1フレームだけ経過時間を捨てる**。シェーダのコンパイルや
  アトラスの構築が丸ごと入っていて数百ミリ秒あるので、そのまま流すと
  開幕で「捨て」が 0.3 秒ほど計上され、何もしていないのに処理落ちして見える

### キーの変更

3D背景のトグルが `3` から **`G`** に移った(`1`〜`4` をシミュレーションレートに使うため)。

### 写経する順番

1. **`GameLoop.cs`** — 実コード 48 行。`Advance` の while ループが本体
2. **`Program.cs` の `Sprite` 構造体** — `Previous*` を2つ足すだけ
3. **`OnUpdate` → `FixedUpdate`** — 何をどちらに置くか(要点4)を意識しながら
4. **`Interpolate` と描画側** — `Render3D` と `RenderSprites` の呼び出しを差し替える
5. **キーハンドラ** — 1/2/3/4・I・L・K
6. **`RunDeterminismCheck`** — 最後でよい。これだけ独立している

## 完成条件

`dotnet run --project reference/Day19 -c Release` で確認する。

### 起動時のコンソール出力

決定性チェックは Y キーでいつでも走らせられる。
**A と B のハッシュが一致し、C だけ違う**ことを確認する。

### 補間の効果(今日の見せ場)

1. **`4`** を押してシミュレーションを 5Hz に落とす
2. **`I`** で補間を切る → スプライトが**1秒に5回しか動かない**。
   タイトルバーの fps は数千のままなので、
   **描画は回っているのにシミュレーションが遅い**状態が見える
3. もう一度 **`I`** → 同じ 5Hz のまま**滑らかに動く**。
   タイトルの `sim 5Hz` と `step:` が変わっていないことを確かめる

`3`(20Hz)でも同じことができる。20Hz は「補間なしだと少しカクつくが、
言われないと気付かないかもしれない」程度なので、
まず 5Hz で違いを確認してから 20Hz を見るとよい。

### 遅れの扱い

1. **`2`** で 60Hz に戻す
2. **`L`** を3回押して負荷を 20000us にする。
   fps が 40 前後まで落ち、タイトルの **`捨て:`** が増えていく
   (シミュレーションがスローモーションになっている)
3. **`K`** で余剰破棄を OFF にする。
   fps がさらに落ち、**`遅れ:` が増え続ける**。もう追いつかない
4. **`K`** で戻し、**`L`** を1回押して負荷を 0 に戻す

### タイトルバーの読み方

```
Day19 - 固定タイムステップ  4049.9 fps | sim 60Hz step:0 α:0.34 遅れ:5.6ms 捨て:0.00s |
補間:ON 負荷:0us 破棄:ON | スプライト:1000 DC:1
```

- `step:0` … このフレームではステップが1回も回らなかった。**描画のほうが速い証拠**
- `α:0.34` … 次のステップまで 34% 進んだところ。補間はこの値を使っている
- `遅れ` … 未処理の時間。1ステップぶん未満なら正常
- `捨て` … 追いつけずに捨てた累計。**負荷をかけていなければ 0.00 のまま**。
  増え続けるなら処理落ち

Day 16〜18 のキー(A / S / B / O / G / Z / C / P / W / F / R / V / Space / Home / F5 / 上下 / Esc)もそのまま使える。

### ビルド

- `dotnet build reference/Day19` が警告 0 で通る
- `dotnet format whitespace reference/Day19/Day19.csproj --verify-no-changes` の差分が 0

## 改造課題

### 課題1(易): 補間を壊して、症状を覚える

要点3の落とし穴を実際に踏む。

1. `UpdateSprites` の `PreviousPosition` の保存を、**移動したあと**に移す。
   5Hz + 補間 ON でどうなるか。原因が分かるか
   (前と後が同じ値になるので、補間しても常に最新の状態が出る)
2. `GameLoop.Advance` の末尾にある `Math.Clamp(..., 0.0, 1.0)` を外し、
   余剰破棄を OFF にして負荷をかける。**α が 1 を超えると補間ではなく外挿**になり、
   スプライトが先へ飛び出す
3. スプライトを「ワープ」させてみる。たとえば 5 秒ごとに
   1枚をランダムな位置へ飛ばす。補間が画面を横切る線を引くのを確認し、
   前の状態を現在値で上書きして直す
4. `SetSimulationRate` から `_loop.Reset()` を消して、
   5Hz から 120Hz へ切り替える。一瞬だけ早送りになるのを確認する

### 課題2(中): 入力を固定ステップに載せる

Day 20 の入力システムへの布石。**入力は描画レートで来るが、
シミュレーションは固定レート**という食い違いを扱う。

1. 矢印キーで動かせるスプライトを1枚足す。
   まず素直に `OnKeyDown` の中で座標を変えてみる。
   シミュレーションレートを 5Hz に落としたとき、**補間がどう壊れるか**
2. 直す。キーの状態を「押されているか」のフラグとして持ち、
   `FixedUpdate` の中で読んで座標を変える
3. 「押した瞬間」(ジャンプなど)はフラグでは表せない。
   `OnKeyDown` でフラグを立て、`FixedUpdate` で消費する形にする。
   **1ステップの間に2回押されたら**どうするか
4. Y キーの決定性チェックが、入力を入れたあとも通るようにする。
   通らないなら、入力のどこかが FixedUpdate の外で状態を変えている

### 課題3(難): 可変ステップとの誤差を定量化する

要点1(a)の「浮動小数の足し算は結合則を満たさない」を数字で見る。

1. スプライト1枚を等速直線運動させ(壁の反射を切る)、
   固定 60Hz で 10 秒ぶん進めた座標と、
   可変(2〜32ms のばらばら)で同じ 10 秒ぶん進めた座標の差を測る
2. 差はどのくらいか。**10秒でどれだけずれるか、100秒では**
3. 反射を戻すと差はどうなるか。**分岐(if)が入ると誤差が非線形に拡大する**
   ——わずかな座標差が「壁に当たったか当たらなかったか」を分ける
4. 速度に重力を足して(`velocity.Y += 9.8f * dt`)、同じ比較をする。
   等速のときより差が大きくなるはず。なぜか
5. 発展: `float` を `double` に変えると差はどうなるか。
   **精度を上げれば決定性が手に入るのか**
   (ヒント: 手に入らない。CPU が違えば `double` でも結果が変わりうる。
   本気でやるなら固定小数点にする。格闘ゲームのロールバック実装がそうしている)

5 が本題。「精度を上げる」と「決定的にする」は**別の問題**で、
後者が欲しいなら浮動小数から離れる必要がある、という話につながる。

## 動作確認済み環境

- .NET SDK 10.0.102 / Windows 11 / NVIDIA GeForce RTX 3070(ドライバ 596.49)
- Silk.NET 2.23.0 / OpenGL 3.3 コアプロファイル
- 960x640 / VSync OFF / Release ビルド / スプライト 1000 枚

### 決定性チェック(200枚 × 600ステップ)

```
A 固定・まとめて      : 7F43863E1E5DFDB8
B 固定・フレーム分割  : 7F43863E1E5DFDB8  一致
C 可変・同じ実時間    : 7444383E9D7F0545  不一致
```

### 補間と負荷(各条件2秒間、スプライト0の画面上の移動量を毎フレーム集計)

| 条件 | fps | 平均移動 | 最大移動 | 停止率 | 遅れ | 捨て |
|---|---|---|---|---|---|---|
| 60Hz 補間ON 負荷0(起動直後) | 3536.4 | 0.047 px | 1.78 px | 0.0% | 13.7 ms | 0.00 s |
| 60Hz 補間ON 負荷0(定常) | 4057.3 | 0.041 px | 0.31 px | 0.0% | 0.5 ms | 0.00 s |
| 5Hz 補間OFF | 4000.6 | 0.039 px | **33.38 px** | **99.9%** | 1.8 ms | 0.00 s |
| 5Hz 補間ON | 3999.3 | 0.042 px | **0.20 px** | **0.0%** | 1.9 ms | 0.00 s |
| 20Hz 補間OFF | 4035.1 | 0.040 px | 8.35 px | **99.5%** | 50.0 ms | 0.00 s |
| 20Hz 補間ON | 4029.4 | 0.041 px | 0.15 px | 0.0% | 0.1 ms | 0.00 s |
| 60Hz 負荷20ms 破棄ON | 41.0 | 3.87 px | 24.01 px | 0.0% | 4.6 ms | **0.20 s** |
| 60Hz 負荷20ms 破棄OFF | **6.2** | 22.25 px | 22.25 px | 0.0% | **354.9 ms** | 0.00 s |

- 補間の有無で**平均移動はほぼ変わらず、停止率と最大移動だけが変わる**
  (総移動量は同じで、分配が変わっている)
- 起動直後の行だけ最大 1.78px が出ている。まったく同じ条件で2回目を測ると
  0.31px なので、これはシェーダのコンパイル等でフレームが1本引っかかったもの
- `遅れ` は計測終了時点の瞬間値。負荷のない条件では1ステップ未満で振動するだけ
  (20Hz の 50.0ms はちょうど1ステップぶんの境界を踏んだ瞬間の値)
- 計測は `reference/Day19` のコードをそのまま使い、
  スプライト0の**補間後の座標**を拾うフックだけを足したコピーで実行した
- `dotnet build` 警告 0 / `dotnet format whitespace --verify-no-changes` 差分 0
