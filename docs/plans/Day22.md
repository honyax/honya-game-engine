# Day 22: GameObject + Component

Phase 4(エンジンコア)の4日目。

## 今日のゴール

スプライトもプレイヤーも `GameObject` になり、**まったく同じ動きを2通りで回して比べられる**。

- `J` キー … 構造体の配列(Day 17 からのやり方)⇔ GameObject + Component(今日のやり方)
- 見た目は完全に同じ。変わるのは更新時間だけで、**2万個で 0.082ms → 1.370ms(17倍)**

この 17 倍が、Day 23 で ECS を書く動機そのものになる。
ロードマップに「**同じものを2回書く**のが狙い」とあるのはこのこと。

あわせて、Day 20 の `UpdatePlayer` と static フィールド6個が
`PlayerController` 1クラスに畳まれて消える。

## 事前に読む資料

ロードマップでは資料の指定が無い日なので、この設計の出所を挙げておく。

- [Game Programming Patterns: Component](https://gameprogrammingpatterns.com/component.html)(Robert Nystrom)
  「継承の木がなぜ破綻するか」から始まって、今日作るものにたどり着く。
  無料で全文が読める。要点1はほぼこの章の要約
- [Game Programming Patterns: Data Locality](https://gameprogrammingpatterns.com/data-locality.html)
  今日測る 17 倍がどこから来るのか。**Day 23 の予習でもある**ので、
  今日のうちに目を通しておくと ECS の話が早い
- [Unity: Order of execution for event functions](https://docs.unity3d.com/Manual/ExecutionOrder.html)
  ライフサイクルの図。今日実装するのはこのうち
  `Awake` / `OnEnable` / `Start` / `FixedUpdate` / `OnDisable` / `OnDestroy` の6つだけ

## 理論の要点

### 1. 継承の木は、組み合わせが増えると必ず破綻する

`Entity` → `MovingEntity` → `Enemy` → `FlyingEnemy` と伸ばしていって、
あるとき「飛ばないが爆発する敵」が要る、となる。

- `ExplodingEnemy` を作る → 爆発の処理が `FlyingEnemy` と重複する
- `Enemy` に上げる → 爆発しない敵まで爆発の処理を持つ

**機能の組み合わせが増えるほど、木のどこに置いても間違いになる**。
「飛ぶ」「爆発する」「毒を撒く」が独立に付いたり付かなかったりするなら、
継承の木は 2^3 通りの葉を要求してくる。

合成なら「爆発する」を1個のコンポーネントにして、必要なものに足すだけ。
**組み合わせは掛け算で増えるが、部品は足し算でしか増えない**。

代わりに払うものが3つある。今日はそれも全部測る。

- 部品どうしが話すには `GetComponent<T>()` が要る(要点6)
- 実行順が決まらない(要点7)
- オブジェクト1個がヒープ上の何個ものオブジェクトになる(要点5)

### 2. `Transform` だけ特別扱いする

`Transform` は `AddComponent` しなくても最初から生えている。
「そこに無いオブジェクト」が考えにくいからで、Unity も Unreal もここは同じ。

そして**親子関係は `Transform` が持つ**。これが今日いちばん効く部分で、

```
ワールド行列 = 自分のローカル行列 * 親のワールド行列
```

を根まで再帰するだけで、砲塔が戦車についてくる。
掛ける順番が「ローカル * 親」なのは `System.Numerics` が行ベクトル規約
(`v' = v * M`)だから。Day 16 で見たのと同じ約束で、先に適用したいものを左に置く。

実演として、根 → 子3つ → それぞれの孫1つ、の3段の木を置いてある
(画面右上の黄色い丸と、そのまわりを回る水色のリング、さらにそのまわりのピンク)。
**子も孫も「原点から半径 R の円周上を回る」としか書いていない**。
どこに出るかは親が決める。

計算し直すのは「変わったときだけ」にしてある(遅延評価)。
値を書き換えたら `MarkDirty` が子孫まで印を付けて回り、
`LocalToWorld` を読まれたときに初めて計算する。
**すでに印が付いていたら子をたどらない**のがコツで、
そうしないと深い木で何度も動かしたときに同じ枝を何度も歩く。

### 3. 回転はクォータニオンで持つ

オイラー角(X回転・Y回転・Z回転の3つの数)で持つと2つ困る。

- **ジンバルロック** … 特定の姿勢で軸が重なり、回転の自由度が1つ消える
- **補間できない** … 2つの姿勢の間を角度ごとに線形補間すると、
  途中で変な方向を向く(最短の回り方にならない)

クォータニオンは4つの数で回転を表し、どちらも起きない。
`System.Numerics.Quaternion` があるので自作はしない。

**合成の順番に罠がある**。実際に行列と突き合わせて確かめると、

| 書き方 | 行列の `M(local) * M(parent)` と一致するか |
|---|---|
| `Quaternion.Concatenate(local, parent)` | 一致する |
| `parent * local`(演算子) | 一致する |
| `local * parent`(演算子) | **一致しない** |

つまり**演算子 `*` は行列と順序が逆**。
行列の掛け算と並べて書くと必ず間違えるので、`Concatenate` を使うほうが安全。

なお 2D(Z 軸まわりだけ)なら、合成は単なる角度の足し算になる
(0.7 と 0.4 を合成すると 1.1 になることを確認済み)。
`Transform.GetInterpolatedWorldRotationZ` はこれを使っている。

### 4. `Awake` と `Start` はなぜ分かれているのか

| 呼ばれるとき | 何を書くか |
|---|---|
| `Awake` | `AddComponent` の**中で**即座に | 自分の初期化だけ |
| `OnEnable` | 有効になったとき | 購読の開始など |
| `Start` | **最初の `FixedUpdate` の直前** | 他のコンポーネントを探す |
| `FixedUpdate` | 毎ステップ | ゲームの状態を変える |
| `OnDisable` / `OnDestroy` | 無効化・破棄のとき | 後始末 |

`Start` が遅れるのには理由がある。
`Start` が走る時点では**そのステップで作られたオブジェクトの `Awake` が全部終わっている**ので、
「相手を探す」処理を安心して書ける。A が B を、B が A を探しても、
どちらの順で作っても動く。`Awake` でやると、先に作られたほうが相手を見つけられない。

もう1つ、`Awake` は `AddComponent` の中で走るので、
**プロパティを設定する前に呼ばれる**。`H` キーの実演で確かめられる。

```
  CreateGameObject + AddComponent
    [obj] Awake       (AddComponent した直後)   ← ラベルがまだ既定値
    [obj] OnEnable
  ↑ ラベルはまだ obj。Awake は AddComponent の中で走るので、
     プロパティを入れる前に呼ばれている
```

「`Awake` の中で、外から設定した値をあてにしてはいけない」がここから出てくる。

### 5. 更新中に消してはいけない

`Destroy` はその場では消さず、**予約するだけ**にしてある。
更新中に `List` から要素を消すと、そこで例外になるか、運が悪いと1個飛ばして更新される。

```
  Destroy を予約 → まだ生きている
    IsDestroyed = True / シーンにはまだ 9 個
  次のステップの終わりで実際に消える
    [demo] OnDisable
    [demo] OnDestroy   (合計 4 ステップ動いた)
    シーンは 8 個になった
```

この「予約して後で」はゲームエンジンのあちこちに出てくる形で、
Day 21 の非同期ロード(復号を予約して、描画スレッドで消化する)も同じ骨格。

まとめて消すときは `RemoveAll` を1回だけ呼ぶ。
1個ずつ `Remove` すると、そのたびに後ろ全部がずれて O(n^2) になる
(ウェーブが全滅する瞬間のような、大量に消える場面で効く)。

### 6. `GetComponent` は「遅い」のか

「`GetComponent` は遅いから `Start` でキャッシュしろ」とよく言われる。実測すると、

| | 1回あたり |
|---|---|
| フィールドを読む | 0.88ns |
| `GetComponent<T>()` 1個目 | 1.03ns |
| `GetComponent<T>()` 2個目 | 2.01ns |

**ほとんど差が無い**。型の判定はせいぜい数個ぶんで、
しかも同じオブジェクトを続けて触るからキャッシュに乗る。

効いてくるのは**別々のオブジェクトを順に引くとき**。
描画パスと同じ形で2万個を1周すると、

| | 1周あたり |
|---|---|
| あらかじめ配列に集めておく | 0.039ms |
| 毎回 `GetComponent` で引く | 0.201ms |

0.16ms の差。60fps の予算 16.6ms に対して 1% なので致命的ではないが、無視もできない。
そして**この差は探索そのものより「オブジェクトを渡り歩くこと」から来ている**
——配列版でも中身は散らばったオブジェクトへの参照で、そこはどちらも同じだからだ。

つまり結論は「`GetComponent` を速くする」ではなく、
**「オブジェクトを渡り歩かない形に変える」**。それが Day 23 の ECS。

### 7. 実行順は決めていない

`Scene` は `_gameObjects` に入っている順(= 生成した順)に更新するだけ。
「敵より先にプレイヤーを動かしたい」という要求が出てきたら、
優先度を持たせるか、フェーズ(入力 → 移動 → 当たり判定)に分けることになる。

**順序が要るのに順序を決めていない設計は、必ずどこかで1フレーム遅れのバグを生む**。
「なぜかカメラが1フレーム遅れてついてくる」の正体はたいていこれ。

Day 23 の ECS では、この点が「システムを順に並べる」という形で最初から明示的になる。
GameObject 方式の弱点が、ECS では設計の前提に変わる。

### 8. 代償はメモリの置き方で払う

同じ動きを2通りで回した実測(1ステップあたり)。

| 個数 | 構造体の配列 | GameObject | 比 | うち枠組み | うち部品の中身 |
|---|---|---|---|---|---|
| 1,000 | 0.020ms | 0.089ms | 4.5x | 0.039ms | 0.050ms |
| 5,000 | 0.032ms | 0.318ms | 9.9x | 0.095ms | 0.223ms |
| 19,981 | 0.082ms | 1.370ms | **16.8x** | 0.614ms | 0.755ms |

**倍率が個数とともに大きくなる**のが肝心なところ。
定数倍の差(仮想呼び出し、null チェック)なら倍率は一定のはず。
増えていくのは**キャッシュに乗り切らなくなるから**で、これがデータ配置の問題である証拠。

「枠組み」は部品の中身を止めて測ったもの(Transform の控え + 走査 + 有効判定)で、
だいたい半分を占める。**部品を軽くしても半分しか減らない**。

メモリも見ておく。

| | 1個あたり | 2万個 |
|---|---|---|
| 構造体の配列 | 64 バイト | 1.3MB |
| GameObject | 515 バイト | 10.0MB |

GameObject + Transform + `List<Component>` 2本 + 部品2個で 515 バイト。
**8倍**で、しかも6個のオブジェクトに分かれてヒープ上に散らばっている。
2万個を GameObject 化するのに 25ms かかるのもここから来る。

念のため確かめておくと、コンポーネント化しても**決定性は保たれる**(Day 20)。

```
A 右へ300ステップ : D3F3CF0BB1B32BC6
B 同じ入力で再実行: D3F3CF0BB1B32BC6  一致
C 左へ300ステップ : 243F758A539EFA8B  不一致
```

置き場所を変えただけで計算は変えていないので、当然そうなる。
**リプレイ(Day 20)は GameObject 化しても壊れない**。

## 前Dayからの差分概要

### 新規ファイル

| ファイル | 行数(うち実コード) | 役割 |
|---|---|---|
| `Transform.cs` | 244 (125) | 位置・回転・スケールと親子関係。ワールド行列の遅延評価と補間 |
| `Component.cs` | 114 (48) | ふるまいの基底。ライフサイクルの6つの入口 |
| `GameObject.cs` | 189 (104) | 名前 + Transform + コンポーネントの入れ物 |
| `Scene.cs` | 220 (131) | 入れ物であり、ライフサイクルを回す人。遅延破棄 |
| `Components.cs` | 312 (174) | 具体的な部品5つ(下表) |

`Components.cs` の中身:

| クラス | 役割 |
|---|---|
| `SpriteRenderer` | 何の絵をどう出すか。**データだけ、ふるまい無し** |
| `BouncingMover` | 画面内を飛び回って端で跳ね返る。`UpdateSprites` と同じ計算 |
| `OrbitMover` | 親のまわりを回る。階層の実演用 |
| `PlayerController` | Day 20 の `UpdatePlayer` をそのまま移したもの |
| `LifecycleLogger` | ライフサイクルの呼ばれ方をコンソールに出すだけ |

### 変更ファイル

| ファイル | 差分 | 内容 |
|---|---|---|
| `Program.cs` | +337 / -141 | シーンの組み立て、2通りの更新の切り替え、シーンの描画。`UpdatePlayer` と static フィールド6個が消えた |

### キーの変更

| キー | 動作 |
|---|---|
| `J` | 更新方式を切り替える(構造体の配列 ⇔ GameObject) |
| `H` | ライフサイクルの実演(コンソール) |
| `Shift` + `PageUp` / `PageDown` | スプライト数を ±10000 |

`PageUp` / `PageDown` は、GameObject モードならシーンのほうも作り直すようになった。
2万個まで 1000 刻みで上げるのは19回かかるので、`Shift` 併用で10倍動くようにしてある。

### 写経する順番

1. **`Transform.cs`** — 親子とワールド行列。`MarkDirty` の早期 return の理由を押さえる
2. **`Component.cs`** — 中身はほぼ空。`Awake` と `Start` のコメントを読む
3. **`GameObject.cs`** — `AddComponent` / `GetComponent` / `ActiveInHierarchy`
4. **`Scene.cs`** — `FixedUpdate` の4段階と `FlushDestroy`
5. **`Components.cs`** — `SpriteRenderer` → `BouncingMover` → `OrbitMover` →
   `PlayerController` → `LifecycleLogger` の順が読みやすい
6. **`Program.cs`** — `BuildScene` → `EnsureSceneSprites` → `FixedUpdate` の分岐 →
   `RenderScene` → `RunLifecycleDemo`

1〜4 は他のファイルに依存しない(`InputSnapshot` だけ知っている)ので、
先に4つ書いてしまうのが楽。

## 完成条件

```
dotnet run --project reference/Day22 -c Release
```

### 起動直後

Day 21 と同じ画面。ただしプレイヤー(黄色い星)は**もう GameObject**で、
画面右上に**階層の実演**が出ている。

- 黄色い丸(根)
- そのまわりを回る水色のリング3つ(子)
- さらにそのまわりを回るピンクの菱形3つ(孫)

コンソールに `シーン構築: GameObject 8 個 / コンポーネント 15 個` が出る。

`PageDown` を何度か押してスプライトを 0 にすると、階層だけがはっきり見える。

### `J`: 更新方式の切り替え

タイトルバーの表示が `構造体配列 更新:0.02ms` から
`GameObject 更新:0.09ms GO:1008 C:2015` に変わる。**絵はまったく変わらない**。

`Shift` + `PageUp` を2回押して2万個まで増やすと差が開く。

```
構造体配列 更新:0.08ms
GameObject 更新:1.37ms GO:19989 C:39977
```

シーンを作り直すたびに、かかった時間とメモリがコンソールに出る。

```
[シーン] スプライト 19981 個を GameObject 化: 25.0ms / 10053KB (515 バイト/個)
```

### `H`: ライフサイクルの実演

`Awake` → `OnEnable` → `Start` → `FixedUpdate` → `OnDisable` → `OnDestroy` の
順と**タイミング**が出る。見るべきは2箇所。

- `Awake` がラベルを設定する**前**に出ていること(要点4)
- `Destroy` を呼んでもその場では消えず、次のステップの終わりで消えること(要点5)

### 階層が効いていること

`J` で GameObject モードにしてから、右上の実演を見る。
リングは黄色い丸のまわりを、菱形はリングのまわりを回っている。
**菱形の位置は「黄色い丸 + リングの円運動 + 菱形の円運動」の合成**で、
`OrbitMover` はそのうち自分のぶんしか書いていない。

### `M` / `N`: リプレイが壊れていないこと

Day 20 と同じように記録・再生できる。プレイヤーがコンポーネントになっても
ハッシュは一致する(要点8)。

## 改造課題

### 課題1(易): 部品を1個足す

画面の端に着いたときに色が変わる `FlashOnBounce` を書いて、
`EnsureSceneSprites` の中で `AddComponent` する。

- `Start` で `SpriteRenderer` を1回引いて持つ
- `BouncingMover` の速度の符号が変わったフレームで色を差す

**`BouncingMover` にも `SpriteRenderer` にも手を入れずに機能が増える**ことを確認する。
これが合成の見返りそのもの。

### 課題2(中): 描画リストを登録制にする

`RenderScene` は毎フレーム全オブジェクトに `GetComponent<SpriteRenderer>()` を呼んでいる。
これを `Scene` 側に `List<SpriteRenderer>` を持たせ、
`RegisterComponent` のときに登録する形に変える(破棄のときに外すのを忘れないこと)。

要点6の 0.16ms が消えるはず。タイトルバーの fps で確かめる。

そのうえで考えてほしいのが、**この「型ごとのリスト」を全種類ぶん作ったらどうなるか**。
それが Day 23 の ECS の入口になる。

### 課題3(難): 実行順を決められるようにする

`Component` に `ExecutionOrder`(既定 0)を足して、`Scene.FixedUpdate` が
その順に呼ぶようにする。要点7の「順序が決まっていない」を解消する。

素直にやると毎ステップ全コンポーネントをソートすることになって遅い。

- コンポーネントが増減したときだけ並べ直す
- そのために `Scene` が全コンポーネントの平坦なリストを持つ
  (今は `GameObject` ごとの入れ子になっている)

まで来ると、**シーンの持ち方が「オブジェクトの木」から「コンポーネントの列」へ
寄っていく**のが分かる。Day 23 はその先にある。

## 動作確認済み環境

- Windows 11 / .NET 10 / Silk.NET 2.23.0
- GL_RENDERER: NVIDIA GeForce RTX 3070/PCIe/SSE2
- 960x640、VSync オフ、`-c Release`、シミュレーション 60Hz

### 1ステップの更新時間(200 ステップの平均)

| 個数 | 構造体の配列 | GameObject | 比 | うち枠組み | うち部品の中身 |
|---|---|---|---|---|---|
| 1,000 | 0.020ms | 0.089ms | 4.5x | 0.039ms | 0.050ms |
| 5,000 | 0.032ms | 0.318ms | 9.9x | 0.095ms | 0.223ms |
| 19,981 | 0.082ms | 1.370ms | 16.8x | 0.614ms | 0.755ms |

「枠組み」は全 `BouncingMover` の `Enabled` を false にして測ったもの。
実行ごとに 1 割ほどばらつく(1,000 個での比は 3.9〜4.8 倍の範囲で観測した)。

### メモリ

| | 1個あたり | 2万個 | 内訳 |
|---|---|---|---|
| 構造体の配列 | 64 バイト | 1.3MB | フィールドの合計(8+8+4+4+4+16+4+4+8+4) |
| GameObject | 515 バイト | 10.0MB | GameObject + Transform + List 2本 + 部品2個 |

`GC.GetTotalAllocatedBytes` で生成前後の差を取った実測値。
2万個の生成そのものに 25ms かかる。

### `GetComponent`

| 同じオブジェクトを繰り返し | 1回あたり |
|---|---|
| フィールドを読む | 0.88ns |
| `GetComponent` 1個目 | 1.03ns |
| `GetComponent` 2個目 | 2.01ns |

| 19,989 個を1周(描画パスと同じ形) | 1周あたり |
|---|---|
| あらかじめ配列に集めておく | 0.039ms |
| 毎回 `GetComponent` で引く | 0.201ms |

### 階層の検算(8項目すべて合格)

```
[OK] 子のワールド座標 = 親 + 子のローカル
[OK] 子は親から半径 86      実際 86.00
[OK] 孫は子から半径 32      実際 32.00
[OK] 親を +100 動かすと孫も +100  実際 100.000
[OK] 親を切ると孫も ActiveInHierarchy = false
[OK] 孫自身の ActiveSelf は true のまま
[OK] 戻すと有効に戻る
[OK] 自分の子孫を親にすると例外
```

### 検証の途中で分かったこと

- **`GetComponent` は思ったより速い**。「遅いからキャッシュしろ」という定説を
  そのまま書こうとしたが、測ると 1.03ns でフィールド読みと大差なかった。
  遅いのは探索ではなく、**散らばったオブジェクトを渡り歩くこと**のほう。
  コメントは測定に合わせて書き直した
- クォータニオンの合成は**演算子 `*` と行列の `*` で順序が逆**。
  `Quaternion.Concatenate(local, parent)` が行列の `M(local) * M(parent)` に対応する。
  行列と並べて書くコードでは事故のもとなので、実際に突き合わせて確認した
- GameObject 版の遅さのうち、`SetLocalRotationZ`(sin/cos とクォータニオン生成)が
  それなりの割合を占める。**汎用の `Transform` を持つこと自体のコスト**で、
  コンポーネント方式そのものの費用ではない。
  比の 16.8 倍を全部「合成のせい」と読むのは正しくない
