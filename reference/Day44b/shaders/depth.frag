#version 330 core

// **何も出力しない画素シェーダ**。今日いちばん短いファイル。
//
// 深度は gl_FragDepth が自動で書かれるので、こちらから書くことは何も無い。
// 出力(out)を1つも宣言していないのは意図的で、
// カラーアタッチメントが1枚も無いフレームバッファ(Framebuffer.CreateDepthOnly)へ
// 描くのだから、色を書く先がそもそも存在しない。
//
// **なぜ空でもファイルが要るのか**。OpenGL の仕様上、
// 画素シェーダを省いたプログラムも作れる(その場合は深度だけが書かれる)が、
// このエンジンの Shader クラスが vert/frag の2本を前提にしているため、
// 器として置いてある。省ける代わりに「省いてよい」ことを覚えておく必要が出るので、
// 1ファイル置くほうが読み手に優しい。
//
// アルファでくり抜く葉のようなものを影に落とすときは、ここが空でなくなる——
// ベースカラーの a を読んで discard する必要がある(Day 40 の透過で扱う)。
void main()
{
}
